name: project
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.9.2 <4.0.0" # Changed from ^3.10.1 to match your system

dependencies:
  flutter:
    sdk: flutter

  # Ismail's State Management
  flutter_riverpod: ^2.5.1
  # Hana's State Management
  provider: ^6.1.5+1

  # Backend & Features
  supabase_flutter: ^2.12.0
  image_cropper: ^8.0.0
  image_picker: ^1.0.7
  cached_network_image: ^3.3.0
  video_player: ^2.10.1
  cloudinary_public: ^0.23.1
  add_2_calendar: ^3.0.1
  confetti: ^0.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/miu_logo.png
    - assets/woman1.jpg
    - assets/woman2.jpg
    - assets/Codementum.png
    - assets/profile.jpg
    - assets/miu_logo1.png
