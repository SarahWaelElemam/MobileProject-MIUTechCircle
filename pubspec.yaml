name: flutter_application_1
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.12.0
  flutter_riverpod: ^2.4.0  # 👈 ADDED FOR STATE MANAGEMENT
  image_picker: ^1.0.7
  file_picker: ^6.1.1
  url_launcher: ^6.2.1
  intl: ^0.18.0 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/men1.jpg